# Changelog

## v1.1.2 (2026-08-16)

### 合规修复
- 🔧 **插件 ID 合规** — `obsidian-wechat-format` → `wechat-format`（Obsidian 官方 manifest 规则：ID 不能包含 "obsidian"）。同步更新安装路径与调试日志路径

## v1.1.1 (2026-08-16)

### 问题修复
- 🔧 **代码块语言栏背景色失效** — 全部改用 `background-color` 长写属性（微信编辑器对 background 简写解析不可靠）；Linux 标题栏去掉 8 位 hex 透明度后缀（微信不识别 `#RRGGBBAA`），default 样式去掉 `opacity: 0.7`（半透明导致背景过淡），Windows 样式语言行补上缺失的背景色

## v1.1.0 (2026-08-01)

### 新增功能
- 🎯 **标题样式系统** — H2/H3/H4 支持 4 种装饰风格：序号图标、心形、分隔线、纯色背景
- 🎨 **代码块边框样式** — 4 种可选边框风格（无边框、圆角、细线、阴影）
- 🍎 **代码块装饰点** — Apple 三色圆点、Linux 灰色方点、Windows 上边栏（SVG 方案，兼容微信编辑器）
- 🖥️ **预览面板工具栏重构** — 两排布局：第一排操作按钮（发送草稿、复制、导出、刷新），第二排样式控件（主题、H2/H3/H4 样式、代码块边框）
- 🌐 **代码语言标签右对齐** — 由默认左上角移至右上角
- 🎚️ **语言栏透明度** — 自适应主题色（`primary` 色 + 60% 透明度）

### 问题修复
- 🔧 **预览不随笔记切换刷新** — 添加 `file-open` 事件监听，切换笔记时自动刷新预览
- 🔧 **预览加载笔记属性（YAML frontmatter）** — 在 `renderToWeChat` 入口处剥离 frontmatter，所有渲染路径受益
- 🔧 **多笔记打开时发送错误内容** — `getEditor()` 按 `activeFile` 匹配编辑器，而非返回第一个查到的编辑器
- 🔧 **代码块背景色缺失** — Nord 主题 `#d8dee9`，温暖文艺 `#f5e6d0` 加深
- 🔧 **代码块 Apple 装饰点过小** — 扩大 SVG 圆点尺寸，确保发送后可见
- 🔧 **装饰点颜色/样式** — 所有装饰元素改为 SVG 方案，通过微信编辑器过滤
- 🔧 **H4 圆点改方点** — 左侧圆点改为灰色方点，并重命名为"左侧方点"
- 🔧 **代码语言标签右对齐** — 由 `text-align: left` 改为 `right`
- 🔧 **预览面板按钮布局** — 操作按钮和样式控件分两排，边缘对齐
- 🔧 **段落空行保留** — 通过 `margin-bottom: 1em` 实现
- 🔧 **分割线显示** — 修复 `---` 不显示导致下方内容消失的问题
- 🔧 **代码清理** — 删除死代码 `const doc = document;`，统一缩进，统一注释格式
- 🔧 **复制按钮绑定** — 提取 `attachCopyHandlers()` 共享方法，消除 `setContent`/`refreshNow` 重复代码
- 🔧 **代码块边框下拉框选中方式** — 统一为 `opt.selected = true` 模式

### 优化
- 📦 编译后体积稳定（~61.9KB）
- ⚡ 预览面板渲染性能优化（缓存 debounce 机制）
- 🧹 代码结构规范化（缩进统一、注释规范、死代码清理）

---

## v1.0.0 (2026-07-26)

**初始正式版** 🎉

- 6 种精美主题（经典商务、清新现代、极简留白、温暖文艺、极客科技、Nord 北欧）
- 实时预览面板，编辑即更新
- 一键复制排版 HTML 到剪贴板
- 导出为独立 HTML 文件
- 直接发送到公众号草稿箱（需配置 API）
- 自定义引言（支持文案、作者署名、字号选择）
- 底部关注区（支持自定义文案、字号选择）
- 首行缩进（可调节缩进大小 1~4em）
- 6 档正文字号（14~18px）+ 5 档行高（1.5~2.0）
- 图片最大宽度设置（80% / 90% / 100%）
- 代码块渲染（语言标签、复制按钮、逐行兼容）
- 引言/关注区支持独立字号（14~20px）
- 关注区文字下方自动添加 14px 空行